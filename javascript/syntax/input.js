// Note: JavaScript doesn't have built-in input like Python
// This would typically be done with readline or process.stdin
// For demonstration, using simulated input

// Simulating map(int, input().split())
let input = "5 10"; // Simulated input
let [p, c] = input.split(' ').map(Number);

//converting number to list of integer
let numInput = "12345"; // Simulated input
let num = Array.from(numInput).map(Number);

//converting string to list
let expInput = "hello"; // Simulated input
let exp = Array.from(expInput); 